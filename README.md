# satellite-telemetry-system
End-to-end satellite telemetry simulation system built with C# and ASP.NET Core that simulates satellite data transmission over a network, processes telemetry through REST APIs, stores data in PostgreSQL and visualizes satellite health and telemetry in a React dashboard.
